name: CI
on:
  push:
    branches: [master]
  pull_request:
    branches: [master]
jobs:
  ci:
    uses: GENIEMARKETING/.github/.github/workflows/ci.yml@main
    secrets: inherit
    with:
      run-build: false
      run-e2e: false
      run-lighthouse: false
