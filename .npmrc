# @vinny/* packages resolve from the org's GitHub Packages registry.
# CI provides NODE_AUTH_TOKEN; locally, export a PAT with read:packages.
@vinny:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${NODE_AUTH_TOKEN}
auto-install-peers=true
